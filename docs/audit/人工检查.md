# 人工检查
存在的问题：
1、身份信息的默认信息;
    请调整身份信息页面的默认值信息：
    默认厂家：hikrobot
    默认名称：amr_your_define
2、身份信息中的导航方式和底盘类型，与后续的底盘信息未作关联；
    请调整：
    1、根据导航方式的选择，产生对应的校验机制，例如：这里选了激光导航，那么后续的传感系统中，就必须选择对应的导航激光雷达，默认关联到导航功能中
    2、根据底盘类型的选择，产生对应的校验机制，例如：这里选了双舵轮那么后续的底盘模型就是双舵轮的底盘模型；
3、底盘参数界面
    底盘参数界面中的运动中心偏移，实际是底盘模块的参数的一部分，可以参考组件库中底盘模块的参数配置中的运动中心参数。我们把它提取到底盘参数界面，就是为了降低信息的复杂性，所以，在界面设计中，就需要把后面的组件库中的底盘这部分的信息去除，采用底盘参数界面的信息呈现，注意，这里的信息来源，仍然是底盘的参数属性，也就是说，多个界面的信息，组合成了一个底盘的模块的信息（身份中底盘类型、参数界面的中心偏移，以及组件库中的底盘模块的其他属性）。所以参数界面中的中心偏移的内容，要包含空载和满载的配置内容，默认二者相同，可以认为调整。

4、组件库：
    底盘模块：底盘模块是没有安装位置这一概念的，因为所有的其他模块的安装位置，都是基于底盘模块的运动中心作为基准位置的，所以底盘模块的安装位置信息要去除；底盘模块由于是基础件，也是没有电气接口资源的，所以底盘的这个界面也要隐去；
    核心主控：该界面是为了体现主控制器和扩展控制板的集合，所以名字可以调整为核心控制板，其中又分为主控制器、扩展控制板等类型；
    接口资源：板子的接口资源请从D:\code\py-sim-web\amr_studio_v4\docs\reference\ModuleLibrary\board_desc 中各个板子的描述文件中进行筛选获取，注意：这些资源将作为这个模块的接口信息，融合到模块的接口属性中去，所以不仅是解析，而且是构建的时候也不要遗漏。并且，注意过滤掉pi/po/lvds等内部接口，仅保留can/ai/ao/di/do/rs485/rs232等类型的接口；
    动力系统：轮组与电机的关联关系，请将轮组、驱动、电机、编码器的关联关系从轮组的属性中抽离出来，对动力系统的界面进行设计重组（注意，只是界面布局的重组，不影响对模块的属性构建，类似底盘参数界面的处理方式）；千万不要影响各个模块的自身属性信息。例如：动力系统中的驱动器，仍然是驱动器，它仍然有自己的属性，不要因为它的属性被提取出来，就改变了它作为驱动器的属性。同理，轮组、电机、编码器也是如此。这块可以从底盘的类型，确定需要的轮组类型（例如差速轮组、立式舵轮组、差速舵轮组），然后根据轮组类型，引导完成驱动、电机、编码器的关联和展示；
    感知避障：感知避障组，不能简单把传感器都纳入到该范围中，例如角度编码器、拉线编码器这些，是属于动力系统的，应该在动力系统中展示和配置；
    需要关注，你可以先把所有的属性参数都展示出来，但是结合各个模块的属性描述字段（是否隐藏，是否必填，是否生效等等），进行区分展示，由我进一步确认，哪些需要你隐藏掉不展示；

    最后，资源库的内容不够完整，请充分参考D:\code\py-sim-web\amr_studio_v4\docs\reference\ModuleLibrary的内容，将完善模块库，并将模块库的资源融合到界面中去。以及将这部分的内容，通过你处理后，变成你的资源库；

    请先基于这些描述进行优化，优化后我们一同验证。

    继续进行问题的分析：

0325：
    现在我开始进行从前端从零开始创建一台车，遇到的问题如下：
    1、底盘参数界面下，调整运动中心偏移时，调整前向距时，后向距离不会结合车体尺寸自动更新；左右同理；
    2、动力聚合配置界面，仅仅是提供了提示，如果是这样，建议不要放在这个界面，而是在添加轮组的时候，结合轮组特点，以图文结合的方式展示呈现；（如果你还无法理解各种轮组的示意图和差异，请你进行网络搜索，理解并设计出根据类型的轮组示意图；
    3、进入到组件库搭建时候，打开资源库，仍然觉得分类粗糙，没有惊喜到这个组件内的子类型区分，同时丢失了搜索功能；
    4、组件库中，轮组组件作为比较特殊的存在，其不仅是一个组件模块清单，还与选择的底盘类型有关，所以在筛选的时候，需要结合底盘类型，筛选出可用的轮组类型的模块；如果没有，应允许手动创建一个模块（后面填写参数）
    5、现有的组件库中的轮组模块，点添加时，系统名称和别名都被描述成了编码器，例如：incrementalEncode，并且实际添加后也是encoder；
    6、选中一个底盘后，其剩余描述的属性（例如最大速度、加速度这些）丢失了（没有显示），并且打开高级配置的选项也没有，请检查；
    
    先解决这些问题，然后我们在继续分析。

    0325 评审问题1：
    新修改的界面，在添加模块时，选择了一个模块，不会弹出确认窗口，而是在选择框的背后，需要把选择框关掉才能点添加确认，这个逻辑不对的，需要解决；
    
         评审问题2：
    在动力系统添加界面，虽然把三种类型用标签的方式区分了，但是完全选择不到匹配的轮组，请你确认是否是资源库中没有，还是筛选条件不对；
    
    评审问题3：
    动力系统这样按照轮组、驱动、电机、编码器进行分类的模式，可以扩展到其他模块的搭建中去，请你思考一下，是否可以这样处理；
    评审问题4：
    你的前端展示可能不稳定，我两次打开同一个组件库的时候，有时候可以看到分类标签，有时候看不到分类标签。
    